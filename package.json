{
  "name": "speedplay",
  "version": "1.0.0",
  "description": "Chrome extension to set YouTube playback speed beyond 2x",
  "private": true,
  "scripts": {
    "lint": "echo 'No linter configured'",
    "build": "echo 'No build step needed — load as unpacked extension in Chrome'"
  }
}
